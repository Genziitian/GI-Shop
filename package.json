{
  "name": "gi-shop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:web": "npm --prefix shop-ledger install",
    "build:web": "npm --prefix shop-ledger run build",
    "build": "npm --prefix shop-ledger install && npm --prefix shop-ledger run build && cp -r shop-ledger/dist ./dist",
    "start": "node server.js"
  }
}
