{
  "name": "gi-shop",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "postinstall": "npm --prefix shop-ledger install && npm --prefix shop-ledger/server install",
    "build": "npm --prefix shop-ledger install && npm --prefix shop-ledger run build && node -e \"require('fs').cpSync('shop-ledger/dist', 'dist', {recursive:true, force:true})\"",
    "start": "node server.js",
    "create-admin": "node shop-ledger/server/create-admin.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "firebase-admin": "^13.6.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.24.3",
    "sqlite3": "^6.0.1"
  }
}
