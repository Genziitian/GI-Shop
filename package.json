{
  "name": "gi-shop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix shop-ledger install",
    "build": "npm --prefix shop-ledger install && npm --prefix shop-ledger run build && node -e \"require('fs').cpSync('shop-ledger/dist', 'dist', {recursive:true, force:true})\"",
    "start": "node server.js"
  }
}
